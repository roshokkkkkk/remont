npm init -y 
npm install -D vite typescript

потом нужно запустить npm run dev и npm start в 2ух терминалах

npm install -D concurrently


# Основные
npm install express express-session mysql2 dotenv

# Dev-зависимости для TypeScript
npm install -D typescript @types/node @types/express @types/express-session ts-node nodemon